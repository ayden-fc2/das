"use client"

import React from "react";
import { Box } from "@mui/material";

const FaultsTracing: React.FC = () => {
    return (
        <Box className={`bg-gray-300 h-full flex flex-col justify-center items-center w-full`}>
            Joined Spaces
        </Box>
    );
};

export default FaultsTracing;
